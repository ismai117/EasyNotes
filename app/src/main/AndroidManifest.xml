<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.easynotes.mynotesapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyNotesApp"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity android:name="com.easynotes.mynotesapp.views.activites.RegisterActivity"/>
        <activity android:name="com.easynotes.mynotesapp.views.activites.LoginActivity" />
        <activity android:name="com.easynotes.mynotesapp.views.activites.MainActivity" />
        <activity android:name="com.easynotes.mynotesapp.views.activites.SplashScreen_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>